{
  "name": "ppq-private-mode",
  "version": "0.3.2",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "description": "End-to-end encrypted AI proxy for PPQ.AI private models. Works standalone or as an OpenClaw plugin.",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "bin": {
    "ppq-private-proxy": "./dist/bin/server.js"
  },
  "scripts": {
    "build": "tsc && node --input-type=module -e \"import{cpSync}from'node:fs';cpSync('lib/nitro','dist/lib/nitro',{recursive:true})\"",
    "start": "npx tsx bin/server.ts",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist",
    "skills",
    "openclaw.plugin.json",
    "README.md"
  ],
  "dependencies": {
    "@peculiar/x509": "^1.14.3",
    "cbor-x": "^1.6.4",
    "ehbp": "^0.2.0",
    "tinfoil": "^1.1.11",
    "zod": "^4.0.0"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.0"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
